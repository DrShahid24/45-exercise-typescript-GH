// Creating a function.
function make_Shirt(size:string, message: string){
    console.log(`You Selected ${size} size shirt with printed ${message} on shirt.`)
};

// Calling the function.
make_Shirt("Mediam", "Shahid Sharif");

